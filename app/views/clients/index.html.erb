<!-- Page Heading -->
<h1 class="h3 mb-2 text-gray-800">Usuarios</h1>
<!-- DataTales Example -->
<div>  
  <%= link_to new_client_path, {remote: true, class: "btn btn-info", id: "anadir-cliente-btn"}  do %>
    <span class="fas fa-plus-square" style=""></span>
  <% end %>
</div>
<br />
<div class="card shadow mb-4">
<div class="card-body">
    <div class="table-responsive">
    <table class="table table-bordered" id="tablaClientes" width="100%" cellspacing="0">
        <thead>
        <tr>
            <th>Nombre</th>
            <th>Dirección</th>
            <th>Email</th>
            <th>Estado de la cuenta</th>
            <th>Tipo de Cuenta</th>
            <th>Acciones</th>
        </tr>
        </thead>
        <tbody>
            <% @clientes.each do |clie| %>
            <tr id="cliente_<%= clie.id %>" >
                <td><%= clie.name %></td>
                <td><%= clie.direction %></td>
                <td><%= clie.email %></td>
                <td><%= t("activerecord.attributes.client.statusClients.#{clie.statusClient}") %></td>
                <td><%= t("activerecord.attributes.client.rolUsers.#{clie.rolUser}") %></td>
                <td>
                <%= link_to "editar", edit_client_path(clie), html_options = {class: "btn btn-info btn-xs", remote: true} %>
                <%= link_to "Borrar", clie, remote: true, method: :delete, data: { confirm: "¿Borrar cliente: #{clie.name}?"}, class: "btn btn-danger btn-xs" %>
                </td>
            </tr>
            <% end %>
        </tbody>
    </table>
    </div>
  </div>
</div>
<%= render 'modal_clientes' %>